const {userService} = require('./userService.js')

/** Services
 * 
 * Services are responsible of handle the operation to modify
 * data in the database. (CRUD operations)
 * 
 */
module.exports = {
    userService,
}